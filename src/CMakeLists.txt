SUBDIRS(linalg)

SET (CODE_TEST_SRCS main.cpp config.cpp)
ADD_EXECUTABLE(ctest ${CODE_TEST_SRCS})

SET(CODE_TEST_LIBS linAlg)
TARGET_LINK_LIBRARIES(ctest ${CODE_TEST_LIBS} ${MPI_LIBS} ${MKL_LIBS} ${BOOST_LIBS})

INSTALL(TARGETS ctest DESTINATION ${CODE_TEST_SOURCE_DIR}/bin)
